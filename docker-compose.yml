services:
  api:
    build: .
    container_name: clientes-api
    ports:
      - "3000:3000"
    env_file:
      - .env   # fornece MONGODB_URI e PORT ao container (o arquivo nunca entra na imagem)
    restart: unless-stopped
